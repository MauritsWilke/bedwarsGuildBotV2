const config = require('../../config.json')

module.exports = {
    name: "",
    description: "",
    example: `example here (no prefix)`,
    aliases: [
        
    ],
    args: false,
    owner: false,
    cooldown: 0 * 1000,
    canTakeIGN: false,

    async run (client, message, args, Discord){
        
    }
}